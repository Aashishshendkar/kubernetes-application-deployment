apiVersion: v1
kind: Service
metadata:
  name: gitops-web-service
  namespace: production
spec:
  type: LoadBalancer
  selector:
    app: gitops-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
